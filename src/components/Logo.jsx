import React from 'react'

// it's for logo icon and we pass width props(width)
function Logo({ width = '100px' }) {
    return (
        <div>
            Mahendra
        </div>
    )
}

export default Logo
